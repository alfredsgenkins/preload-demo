# Block lazy-load DEMO (proof of concept)

## Requirements
- Nodemon installed globaly `npm i -g nodemon`

## Run instructions
- run on local server: `npm run serve`
- open in browser: `127.0.0.1:8999`
